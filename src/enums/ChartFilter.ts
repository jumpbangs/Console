enum LeftFilter {
  CARD_VOLUME = 'Card Volume',
  TOTAL_SALES = 'Total Sales',
}

export enum RightFilter {
  HOURLY = 'hour',
  WEEKLY = 'week',
  DAILY = 'day',
  MONTHLY = 'month',
}

export default LeftFilter;
